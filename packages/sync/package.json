{
  "name": "@hebrew-dates/sync",
  "version": "0.1.0",
  "private": true,
  "description": "Destination-agnostic reconciliation planning. Pure: no network, no database.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hebrew-dates/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^5.0.1",
    "@types/node": "^22.15.3"
  }
}
