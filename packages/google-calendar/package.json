{
  "name": "@hebrew-dates/google-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "Google Calendar event payload mapping. Pure: no network, no credentials.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hebrew-dates/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "@types/node": "^22.15.3"
  }
}
